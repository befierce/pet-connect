// This file is no longer needed as we're using real Supabase data
// Keeping it for reference but it won't be imported anywhere

export const mockBookings = [];
export const generateMockBookings = () => [];